{
  "name": "buildahcker-alpine-sample",
  "type": "module",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.12.2",
    "buildahcker": "0.4.0",
    "tsx": "^4.7.1"
  },
  "scripts": {
    "build": "buildah unshare tsx build.ts",
    "start": "qemu-system-x86_64 -m 1024M -enable-kvm -machine q35 -device intel-iommu -drive file=output/disk.img,format=raw,if=virtio,readonly=on -nic user,model=virtio-net-pci"
  }
}
